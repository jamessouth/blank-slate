import React from 'react';


export default function Lobby() {
   return (
        <div>
            <button>start</button>
            <button className="flex">join</button>

            
        </div>
    );
}